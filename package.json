{
  "name": "actionbook-project",
  "version": "0.0.0",
  "private": true,
  "description": "Actionbook - The Action Engine",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.24.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "changeset": "changeset",
    "version-packages": "changeset version && node scripts/sync-versions.js",
    "build:release": "turbo build --filter=@actionbookdev/sdk... --filter=@actionbookdev/mcp... --filter=@actionbookdev/tools-ai-sdk... --filter=@actionbookdev/json-ui...",
    "release": "echo 'Publishing is handled by CI. Use pnpm changeset to create a changeset.'",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.29.8",
    "turbo": "^2.6.3"
  },
  "pnpm": {
    "overrides": {
      "@neondatabase/serverless": "^0.10.4"
    }
  },
  "dependencies": {
    "@types/dom-chromium-ai": "^0.0.14"
  }
}

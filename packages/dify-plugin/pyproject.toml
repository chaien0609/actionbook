[project]
name = "actionbook-dify-plugin"
version = "0.1.0"
description = "Actionbook plugin for Dify - Browser automation manuals"
authors = [
    {name = "Actionbook Team", email = "support@actionbook.dev"}
]
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}
keywords = ["dify", "plugin", "browser-automation", "web-scraping", "testing"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "requests>=2.31.0",
    "dify-plugin>=0.5.1,<0.6.0",
    "playwright>=1.40.0",
    "hyperbrowser>=0.1.0",
]

[project.urls]
Homepage = "https://actionbook.dev"
Documentation = "https://docs.actionbook.dev"
Repository = "https://github.com/actionbook/actionbook"
Issues = "https://linear.app/cue-labs"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "A", "C4", "SIM"]
ignore = ["E501"]  # Line too long

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
addopts = "-v --cov --cov-report=term-missing -m 'not integration and not e2e'"
markers = [
    "integration: tests that make real HTTP calls to api.actionbook.dev",
    "e2e: end-to-end tests requiring a running Dify instance",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "pytest-timeout>=2.3.1",
    "ruff>=0.15.0",
    "tenacity>=8.0.0",
]
